# TypeScript

TypeScript의 타입 시스템, 제네릭, 타입 추론, 타입 안정성과 관련된 질문을 정리합니다.

## 질문 목록

- TypeScript를 사용하는 이유는 무엇인가요?
- `type`과 `interface`의 차이는 무엇인가요?
- `unknown`과 `any`는 어떻게 다른가요?
- 제네릭은 어떤 상황에서 사용하나요?
- `satisfies` 키워드는 언제 유용한가요?

