# Java

JVM, 객체 지향, 컬렉션, 동시성, GC와 관련된 Java 면접 질문을 정리합니다.

## 질문 목록

- JVM은 어떤 구조로 동작하나요?
- GC는 언제 발생하고 어떤 방식으로 객체를 정리하나요?
- `equals`와 `hashCode`를 함께 재정의해야 하는 이유는 무엇인가요?
- `HashMap`과 `ConcurrentHashMap`의 차이는 무엇인가요?
- Java의 예외 처리 방식과 checked exception의 장단점은 무엇인가요?

