# Kotlin

Kotlin의 null safety, 코루틴, 함수형 문법, Java 상호 운용성과 관련된 질문을 정리합니다.

## 질문 목록

- Kotlin의 nullable 타입은 NPE를 어떻게 줄이나요?
- `data class`는 어떤 메서드를 자동으로 제공하나요?
- `object`와 `companion object`는 언제 사용하나요?
- 코루틴은 스레드와 무엇이 다른가요?
- Java 코드와 함께 사용할 때 주의할 점은 무엇인가요?

